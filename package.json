{
  "name": "dev-cms",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "project"
  ],
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "scripts": {
    "build": "npm run build:shared && cd server && npm run build",
    "build:shared": "cd shared && npx tsc -b",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "@types/mime-types": "^3.0.1",
    "@types/react": "^19.2.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.926.0",
    "@aws-sdk/lib-storage": "^3.926.0",
    "@sendgrid/mail": "^8.1.6",
    "mime-types": "^3.0.1",
    "sharp": "^0.34.5"
  }
}

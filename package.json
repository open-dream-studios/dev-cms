{
  "name": "dev-cms",
  "private": true,
  "workspaces": ["shared", "server", "project"],
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "scripts": {
    "build": "npm run build:shared && cd server && npm run build",
    "build:shared": "cd shared && npx tsc -b",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "typescript": "^5.9.3"
  }
}

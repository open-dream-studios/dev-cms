[build]
builder = "NIXPACKS"
buildCommand = "npm run build"
installCommand = "npm ci"
[build.env]
NODE_VERSION = "20"

[start]
command = "npm start"

[variables]
NODE_ENV = "production"
NIXPACKS_NODE_VERSION = "20"
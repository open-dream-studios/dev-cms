{
  "buildCommand": "cd shared && npm install && npm run build && cd ../project && npm install && npm run build",
  "installCommand": "echo 'handled by buildCommand'",
  "outputDirectory": "project/.next",
  "framework": "nextjs"
}